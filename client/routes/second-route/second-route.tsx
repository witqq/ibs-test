import {ObservableComponent} from "../../utils/mobx/ObservableComponent";
import * as React from "react";

export class SecondRoute extends ObservableComponent {
  render() {
    return (
      <div>
        Second route
      </div>
    );
  }
}